curl -L https://git.io/get_helm.sh | bash
helm init
helm repo update
helm install stable/wordpress -n myblog
kubectl get all --all-namespaces
kubectl edit service/myblog-wordpress # Change LoadBalancer to NodePort
echo "WordPress URL is: http://"$(minikube ip)":"$(kubectl get svc myblog-wordpress -o=jsonpath='{.spec.ports[?(@.port==80)].nodePort}')

